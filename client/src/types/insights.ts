export type GenreAvg = {
  genre: string;
  avg_rating: number;
};

export type GenreCount = {
  genre: string;
  num_ratings: number;
};

export type AuthorAvg = {
  author: string;
  avg_rating: number;
};

export type AuthorCount = {
  author: string;
  num_ratings: number;
};
